const P=window.NOMAIRE_PRODUCTS||[];let cart=[];const grid=document.querySelector('#products'),filters=document.querySelector('#filters');const cats=['All',...new Set(P.map(p=>p.category))];filters.innerHTML=cats.map((c,i)=>`<button class="filter ${i===0?'active':''}" data-cat="${c}">${c}</button>`).join('');function render(cat='All'){grid.innerHTML=P.filter(p=>cat==='All'||p.category===cat).map(p=>`<article><div class="product-img"><img src="${p.image}" alt="${p.name}" loading="lazy"></div><div class="product-info"><span class="cat">${p.category}</span><h3>${p.name}</h3><p>${p.description}</p><div class="price-row"><strong>$${p.price.toFixed(2)}</strong><button class="add" data-id="${p.id}">Add to bag</button></div></div></article>`).join('')}render();filters.addEventListener('click',e=>{if(!e.target.matches('.filter'))return;document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));e.target.classList.add('active');render(e.target.dataset.cat)});const bag=document.querySelector('#bag'),shade=document.querySelector('#shade');function openBag(){bag.classList.add('open');shade.classList.add('show')}function closeBag(){bag.classList.remove('open');shade.classList.remove('show')}document.querySelector('#bagOpen').onclick=openBag;document.querySelector('#bagClose').onclick=closeBag;shade.onclick=closeBag;document.addEventListener('click',e=>{if(e.target.matches('.add')){cart.push(P.find(p=>p.id==e.target.dataset.id));updateBag();openBag()}if(e.target.matches('[data-remove]')){cart.splice(+e.target.dataset.remove,1);updateBag()}});function updateBag(){document.querySelector('#bagCount').textContent=cart.length;const total=cart.reduce((s,p)=>s+p.price,0);document.querySelector('#bagTotal').textContent='$'+total.toFixed(2);document.querySelector('#bagItems').innerHTML=cart.length?cart.map((p,i)=>`<div class="bag-item"><img src="${p.image}" alt=""><div><strong>${p.name}</strong><small>$${p.price.toFixed(2)}</small></div><button data-remove="${i}">Remove</button></div>`).join(''):'<p style="color:#69736b;padding:25px 0">Your bag is ready for something good.</p>'}updateBag();document.querySelector('#checkout').onclick=()=>alert('Checkout demo — connect your preferred payment provider before launch.');